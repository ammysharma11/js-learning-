console.log(null>0);
console.log(null==0); //== work as equality so it doesn't compare
console.log(null>=0); // it just compares the value it converts the null to zero

console.log(undefined==0);
console.log(undefined>=0);
console.log(undefined<0);


console.log("2"===2)  // ===  does not convert the data type it just compaers if datatype are also same then the value
 