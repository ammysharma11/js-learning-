function one()
{
    const username='ammy'

    function two()
    {
        const website = "youtube"
        console.log(username)
    }
    //console.log(website);   
    two()
}
//one();
//console.log(one());
if(true)
{
    const username = "ammy"
    if(username=="ammy")
    {
        const website="youtube"
        console.log(username+website);

    }
    
}

// +++++++++++++INTERESTING ++++++++++++++

 addone(5);

function addone(num)
{
    return num+1;

}

addTwo(5) // when we define function this way we can't  access it before intializtion

const addTwo=function(num)
{ 
    return num +2;
}



